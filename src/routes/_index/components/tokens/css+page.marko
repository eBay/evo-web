import { urls } from "../../../../data";

<div id="tokens">
  <section-header/>
  <p>
    Design tokens are the CSS custom properties that carry every color,
    spacing, and other design values used across Evo Web's components, for
    example
    <code>--color-background-accent</code>
    or
    <code>--color-foreground-accent</code>. A theme is built by overriding this same set of tokens, so every
    component picks up the new values without any component code changing.
    The tables below are the full set of tokens available today; to learn
    how to build your own theme or apply a subtheme, see the Theming Guide.
  </p>
  <p>
    <a class="fake-btn fake-btn--secondary" href=`${urls.guides}/theming`>
      Theming Guide
    </a>
  </p>

  <h3>Evo Tokens</h3>

  <p>
    Core, light and dark token sets are individually exposed via the following
    submodules:
  </p>
  <dl>
    <dt>
      <span class="secondary-text">
        @ebay/skin/tokens/
      </span>
      evo-core
    </dt>
    <dd>eBay default primitives</dd>
    <dt>
      <span class="secondary-text">
        @ebay/skin/tokens/
      </span>
      evo-light
    </dt>
    <dd>eBay semantic aliases for light mode</dd>
    <dt>
      <span class="secondary-text">
        @ebay/skin/tokens/
      </span>
      evo-dark
      <sup>*</sup>
    </dt>
    <dd>eBay semantic aliases for dark mode</dd>
  </dl>
  <p>
    <sup>*</sup>
    This sub-module will not be included automatically by its parent
    module; it must be explicitly included by any page that is itself dark-mode
    compatible.
  </p>

  <p>
    Looking to build your own theme, apply a subtheme, or see what themes
    ship today (like Evo Live)? See the
    <a href=`${urls.guides}/theming`>
      Theming Guide
    </a>.
  </p>

  <h3 id="tokens-color-semantic">
    Semantic Color Tokens
  </h3>
  <p>Note: To see the different theme colors, use the theme switcher</p>
  <div class="demo">
    <div class="demo__inner">
      <token-doc type="semantic"/>
    </div>
  </div>

  <h2>Core Expressive tokens</h2>
  <p>
    Core expressive tokens are a subset of core tokens which can be used in
    marketing or other related categories
  </p>
  <div class="demo">
    <div class="demo__inner">
      <token-doc type="expressive"/>
    </div>
  </div>

  <details class="details">
    <summary class="details__summary">
      <h3 class="details__label">
        Primitive Color Tokens
      </h3>
      <span class="details__icon" hidden>
        <svg class="icon icon--16" aria-hidden="true">
          <use href="#icon-chevron-down-16"/>
        </svg>
      </span>
    </summary>
    <p>
      <strong>NOTE:</strong>
      Primitive tokens are not intended to be used by the application;
      instead, they are utilized by the semantic tokens.
    </p>
    <div class="demo">
      <div class="demo__inner">
        <token-doc type="core"/>
      </div>
    </div>
  </details>
</div>
export const metadata = {
  component: "tokens",
  "ds-component": {
    version: "1.0",
  },
};
