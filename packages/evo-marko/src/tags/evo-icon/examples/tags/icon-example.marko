export interface Input {
  name: string;
  content: Marko.Body;
}

<style/styles>
  .cell {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 72px;
  }

  .cell .tip {
    display: flex;
    width: 100%;
    height: 100%;
  }

  .cell .tip > button {
    width: 100%;
    height: 100%;
    border-radius: 8px;
  }
</style>

<div class=(styles as any).cell>
  <evo-infotip
    class=(styles as any).tip
    a11yIconText=input.name
    a11yCloseText="Dismiss"
  >
    <@icon>
      <${input.content}/>
    </@icon>
    ${`<${input.name}/>`}
  </evo-infotip>
</div>
