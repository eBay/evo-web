import type { Input as CalendarInput } from "<evo-calendar>";

<let/selected=undefined as undefined | CalendarInput["selected"]>

<p>Selected: ${typeof selected === "string" ? selected : JSON.stringify(selected) ?? "none"}</p>

<evo-calendar
    selectMode="day"
    ...input
    selected:=selected
    a11yNavigateText(month, dir) { return `Go to ${month} (${dir === "prev" ? "Previous" : "Next"} month)` }
/>
