<div id="selection-chip">
    <section-header metadata=metadata/>

    <p>A selection chip is an ARIA toggle button that applies or unapplies its value to a section of a form or application.</p>
    <p>Selection chips typically appear in groups (e.g. "Recommended Categories").</p>
    <p>When selection chip loads, add <code>selection-chip--loading</code> so that the animation does not trigger on page load. This should be removed after the component is mounted.</p>
    <div class="demo">
        <div class="demo__inner">
            <button class="selection-chip selection-chip--loading" type="button" aria-pressed="false">
                <span class="selection-chip__text">
                    Football
                </span>
                <svg
                    class="selection-chip__trailing icon icon--12"
                    width="13"
                    height="12"
                >
                    <icon-symbol name="close-12"/>
                </svg>
            </button>
            <span style="margin-right: 8px;"/>
        </div>
    </div>
    <highlight-code type="html">
        <button class="selection-chip" type="button" aria-pressed="false">
            <span class="selection-chip__text">
                Football
            </span>
            <svg class="selection-chip__trailing icon icon--12" width="13" height="12">
                <use href="icons.svg#icon-close-12"/>
            </svg>
        </button>
    </highlight-code>
</div>
export const metadata = {
    component: "selection-chip",
    "ds-component": {
        name: "input-chip",
        version: "1.0.0",
    },
};
