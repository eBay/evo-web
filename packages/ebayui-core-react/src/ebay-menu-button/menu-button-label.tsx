import React, { FC, ReactNode } from "react";

const EbayMenuButtonLabel: FC<{ children?: ReactNode }> = ({ children }) => <>{children}</>;

EbayMenuButtonLabel.displayName = "EbayMenuButtonLabel";

export default EbayMenuButtonLabel;
