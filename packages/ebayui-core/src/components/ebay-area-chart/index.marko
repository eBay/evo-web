import { processHtmlAttributes } from "../../common/html-attributes";
$ const {
    data,
    class: inputClass,
    series,
    title,
    description,
    tooltipValueFormatter,
    tooltipTitleFormatter,
    xLabelFormat,
    xLabelFormatter,
    xPositioner,
    yLabels,
    yLabelFormatter,
    yPositioner,
    xAxisLabelFormat,
    xAxisPositioner,
    yAxisLabels,
    yAxisPositioner,
    highchartOptions,
    ...restInput
} = input;

<div ...processHtmlAttributes(restInput) class=[inputClass, "ebay-area-chart"]>
    <div id=component.getContainerId() class="ebay-area-chart__container"/>
</div>
