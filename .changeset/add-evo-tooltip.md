---
"@evo-web/react": patch
"@evo-web/marko": patch
---

Add EvoTooltip component with an EvoButton host by default, and default EvoButton to `type="button"`. Evo Tooltip now uses one hover-and-focus trigger model in both experimental packages, so the `noHover` option is removed.
