---
"@ebay/ebayui-core": minor
---

Add a generic `@media` attribute tag to `ebay-card` for arbitrary media content (e.g. `<video>`). When present it renders in place of `@image`, which is now optional.
