---
"@evo-web/react": patch
---

Introduce the React EvoCombobox filtering API with `filterMethod="auto"` as the default. The experimental component exposes automatic, application-managed, and input-independent filtering modes while keeping native browser autocomplete disabled.
