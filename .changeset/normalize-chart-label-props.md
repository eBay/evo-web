---
"@ebay/ebayui-core": minor
"@ebay/ui-core-react": minor
---

Normalize chart customization APIs with `xLabelFormat`, `xLabelFormatter`, `xPositioner`, `yLabels`, `yLabelFormatter`, and `yPositioner`; support `tooltipValueFormatter` and `tooltipTitleFormatter` across all charts; and use `description` consistently for chart accessibility descriptions. Existing axis-prefixed properties and donut `highchartsDescription` remain supported as deprecated aliases.
